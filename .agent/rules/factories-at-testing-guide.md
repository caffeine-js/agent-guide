---
trigger: always_on
---

When creating a generator method for a domain entity, you can use `import { makeEntityFactory } from "@caffeine/models/factories";`. Its return value is what the validator expects.